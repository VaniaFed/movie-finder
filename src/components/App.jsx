import React from 'react';
import { Button } from 'components/button';

export const App = () => {
    return (
        <div className="container">
            <Button text="Hello" />
        </div>
    );
};
