import PropTypes from 'prop-types';

export const ControlsDataPropTypes = {
    search: PropTypes.string,
    searchBy: PropTypes.string,
    sortBy: PropTypes.string,
    sortOrder: PropTypes.string
};
